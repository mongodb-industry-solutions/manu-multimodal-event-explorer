FROM node:22-alpine
WORKDIR /app
COPY frontend/package*.json ./
RUN npm install
COPY frontend/ .
# Empty string forces relative /api URLs in browser
ENV NEXT_PUBLIC_API_URL=""
RUN npm run build
ENV PORT=8080
EXPOSE 8080
CMD ["npm", "start"]